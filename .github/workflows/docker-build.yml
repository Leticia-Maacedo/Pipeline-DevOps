name: CI/CD Pipeline

on:
push:
branches: [ "main" ]

jobs:
build:
runs-on: ubuntu-latest
steps:
- name: Checkout do código
uses: actions/checkout@v3

```
  - name: Configurar Docker Buildx
    uses: docker/setup-buildx-action@v3

  - name: Construir imagem Docker
    run: docker build -t minha-api:latest .

  - name: Verificar imagem criada
    run: docker images
```

test:
runs-on: ubuntu-latest
needs: build
steps:
- name: Checkout do código
uses: actions/checkout@v3

```
  - name: Configurar Python
    uses: actions/setup-python@v4
    with:
      python-version: '3.11'

  - name: Instalar dependências
    run: |
      python -m venv venv
      source venv/bin/activate
      pip install -r requirements.txt

  - name: Rodar testes
    run: |
      source venv/bin/activate
      pytest tests/
```

deploy:
runs-on: ubuntu-latest
needs: test
steps:
- name: Checkout do código
uses: actions/checkout@v3

```
  - name: Deploy para Render
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
    run: |
      echo "Iniciando deploy no Render..."
      curl -X POST https://api.render.com/deploy/srv-${SERVICE_ID} \
      -H "Authorization: Bearer $RENDER_API_KEY"
      echo "Deploy solicitado com sucesso!"
```
# test commit para rodar workflow
